<% include ../partials/head %>


    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Password</th>
            </tr>
        </thead>
        <tbody>
          <% rows.forEach(function(rows){ %>
            <tr>
                <td><%= rows.title %></td>
                <td scope="row"><%= rows.code %></td>
                <td><%= rows.credits %></td>
                <td><%= rows.sections %></td>
                 <td><%= rows.title %></td>
            </tr>
            <% }); %>
        </tbody>
    </table>


<% include ../partials/footer %>